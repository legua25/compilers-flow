def isPrime(n: Int, d: Int) =
  if (n <= 1)
    false
  else if (n == d)
    true
  else if (n % d == 0)
    false
  else
    isPrime(n, d + 1)

def isPrime(n: Int) =
  isPrime(n, 2)

val n = readLine().toString

for (i <= 2 to n)
  if (isPrime(i))
    printLine(i)
