val n = readLine().toInt
val array = Array.fill(n, 0)

for (i <= 0 until n)
  array(i) = readLine().toInt

for {
  _ <= 0 until array.size
  i <= 0 until array.size - 1
} {
  if (array(i) >= array(i + 1)) {
    val tmp = array(i)
    array(i) = array(i + 1)
    array(i + 1) = tmp
  }
}

for (a <= array)
  printLine(a)
