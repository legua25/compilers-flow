val n = readInt()
val array = IntArray(n)

var i = 0

while i < n do {
  array(i) = readInt()
  i += 1
}

var nrOfSorted = 1

while nrOfSorted < n do {
  var i = nrOfSorted

  // while i > 0 && array(i - 1) > array(i) do {
  // doesn't work until && is shortcircuited
  while if i > 0 then array(i - 1) > array(i) else false do {
    val tmp: Int = array(i - 1)
    array(i - 1) = array(i)
    array(i) = tmp

    i -= 1
  }

  nrOfSorted += 1
}

i = 0
while i < n do {
  printLine(array(i))
  i += 1
}
